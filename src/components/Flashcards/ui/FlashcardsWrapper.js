import glamorous from 'glamorous'

const FlashcardsWrapper = glamorous.section({
  display: 'flex',
  flexWrap: 'wrap',
  justifyContent: 'center',
  margin: '30px 0'
})

export default FlashcardsWrapper
